{{ render '@header' }}

{{#extend '@main' }}
  {{#content 'main-content' }}
    {{#extend '@grid-container--full-width'}}
      {{#content 'grid-container-content'}}
        {{ render '@wall' }}
      {{/content}}
    {{/extend}}
  {{/content}} 
{{/extend}}
